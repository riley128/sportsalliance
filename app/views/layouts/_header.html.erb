<nav class="navbar navbar-default .navbar-fixed-top nav-shadow" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <%= link_to image_tag("sports-alliance-logo.jpg"), root_path, :class=>"navbar-logo" %>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse navbar-ex1-collapse">
    <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            <%= current_user.first_name.upcase %>  <span class="glyphicon glyphicon-chevron-down"></span>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to 'SETTINGS', edit_user_registration_path %></li>
            <li><%= link_to "SIGN OUT", destroy_user_session_path, method: :delete %></li>
          </ul>
        </li>
      <% else %>
        <li><%= link_to "", user_omniauth_authorize_path(:facebook), :class=>"facebook-login" %>
      <% end %>
    </ul>
  </div><!-- /.navbar-collapse -->
</nav>